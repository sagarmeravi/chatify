{
  "name": "chatify",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run build && cross-env NODE_ENV=production npm run start --prefix backend",
    "start:dev": "npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "license": "ISC",
  "description": ""
}